package com.checkin.partner.ui.theme

import androidx.compose.ui.graphics.Color

// ── 主色系 ──
val PinkPrimary     = Color(0xFFD87093)  // 主品牌柔粉
val PinkContainer   = Color(0xFFFAEEF2)  // 主色浅透底色 rgba(216,112,147,0.12) on white
val PinkOnContainer = Color(0xFFB35A7A)  // 主色容器上文字

// ── 辅助色系 ──
val CreamLight      = Color(0xFFFFEFD9)  // 辅助奶油浅黄 #FFE8C8 @70% on white
val CreamContainer  = Color(0xFFFFF4E4)  // 奶油色容器底

// ── 完成/成功色 ──
val MintGreen       = Color(0xFF95D5B2)  // 完成薄荷绿
val GreenContainer  = Color(0xFFE8F5EE)  // 绿色浅透底

// ── 页面底色 ──
val PageBg          = Color(0xFFFFFFFF)  // 页面底色纯白
val CardBg          = Color(0xFFF7F8FA)  // 通用卡片底色
val CardBgDark      = Color(0xFF2C2C2E)  // 深色模式卡片底

// ── 分割线 ──
val DividerColor    = Color(0xFFEFF2F5)  // rgba(203,213,224,0.4) on white

// ── 文字色 ──
val TextDark        = Color(0xFF2D3748)  // 标题深色文字
val TextGray        = Color(0xFF718096)  // 辅助中灰文字
val TextLight       = Color(0xFFCBD5E0)  // 次要浅灰文字

// ── 功能色 ──
val DangerRed       = Color(0xFFC53030)  // 危险色
val SuccessGreen    = Color(0xFF48BB78)  // 成功绿色

// ── 底部导航 ──
val BottomNavActive   = PinkPrimary
val BottomNavInactive = TextLight

// ═══════════════════════════════════════════
// 深色模式色值
// ═══════════════════════════════════════════
val DarkBg          = Color(0xFF1C1B1F)
val DarkSurface     = Color(0xFF2C2C2E)
val DarkSurface2    = Color(0xFF3A3A3C)
val DarkTextPrimary = Color(0xFFF0E6EC)
val DarkTextGray    = Color(0xFFA8A0A8)
val DarkDivider     = Color(0xFF3A3A3C)
