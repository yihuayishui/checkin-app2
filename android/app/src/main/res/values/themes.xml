<?xml version="1.0" encoding="utf-8"?>
<!-- 打卡搭档 -->
<resources>
    <style name="Theme.CheckinPartner" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- 极光推送 JNotifyActivity 所需主题 -->
    <style name="JPushTheme" parent="android:Theme.Translucent.NoTitleBar" />
</resources>
