<?xml version="1.0" encoding="utf-8"?>
<!-- 打卡搭档 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 极光推送所需权限 -->
    <permission
        android:name="com.checkin.partner.permission.JPUSH_MESSAGE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.checkin.partner.permission.JPUSH_MESSAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- 极光推送所需权限 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <application
        android:name=".CheckinApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="打卡搭档"
        android:supportsRtl="true"
        android:theme="@style/Theme.CheckinPartner"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.CheckinPartner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Firebase 消息服务 -->
        <service
            android:name=".network.fcm.CheckinFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- 极光推送 -->
        <meta-data
            android:name="JPUSH_APPKEY"
            android:value="e3b1041d3b96231eb2aed6c0"
            tools:replace="android:value" />
        <meta-data
            android:name="JPUSH_CHANNEL"
            android:value="developer-default"
            tools:replace="android:value" />

        <!-- JCore 必要组件 -->
        <service
            android:name="cn.jpush.android.service.JCommonService"
            android:foregroundServiceType="remoteMessaging"
            android:exported="false"
            android:process=":jcore">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE" />
            </intent-filter>
        </service>
        <service
            android:name="cn.jpush.android.service.PushService"
            android:foregroundServiceType="remoteMessaging"
            android:exported="false"
            android:process=":pushcore">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.REGISTER" />
                <action android:name="cn.jpush.android.intent.REPORT" />
                <action android:name="cn.jpush.android.intent.PushService" />
                <action android:name="cn.jpush.android.intent.PUSH_TIME" />
            </intent-filter>
        </service>
        <service
            android:name="cn.jpush.android.service.DaemonService"
            android:foregroundServiceType="remoteMessaging"
            android:exported="false"
            android:process=":daemon" />
        <receiver
            android:name="cn.jpush.android.service.PushReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.NOTIFICATION_RECEIVED_PROXY" />
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="cn.jpush.android.service.AlarmReceiver"
            android:exported="false" />
        <receiver
            android:name="cn.jpush.android.service.WakedResultReceiver"
            android:exported="false" />
        <activity
            android:name="cn.jpush.android.ui.PushActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="cn.jpush.android.ui.PushActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name="cn.jpush.android.ui.PopWinActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name="cn.jpush.android.service.JNotifyActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/JPushTheme">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.JNotifyActivity" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="${applicationId}" />
            </intent-filter>
        </activity>
        <provider
            android:name="cn.jpush.android.service.DataProvider"
            android:authorities="com.checkin.partner.JPush.DataProvider"
            android:exported="false" />
        <provider
            android:name="cn.jpush.android.service.DownloadProvider"
            android:authorities="com.checkin.partner.JPush.DownloadProvider"
            android:exported="false" />
        <provider
            android:name="cn.jpush.android.service.InitProvider"
            android:authorities="com.checkin.partner.jiguang.InitProvider"
            android:exported="false"
            android:readPermission="com.checkin.partner.permission.JPUSH_MESSAGE"
            android:writePermission="com.checkin.partner.permission.JPUSH_MESSAGE" />
        <receiver
            android:name=".network.jpush.JPushReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="cn.jpush.android.intent.RECEIVE_MESSAGE" />
                <category android:name="${applicationId}" />
            </intent-filter>
        </receiver>

        <!-- 通知划出删除时标记已读（不启动 Activity） -->
        <receiver
            android:name=".network.fcm.NotificationDismissReceiver"
            android:exported="false" />
    </application>
</manifest>
